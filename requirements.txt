# Core
fastapi>=0.138.1
uvicorn[standard]>=0.49.0
python-multipart>=0.0.12
aiosqlite>=0.22.1
aiofiles>=25.1.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
requests>=2.31.0
pillow>=10.4.0
numpy>=1.26.0
einops>=0.7.0
huggingface_hub>=0.26.0
psutil>=5.9.0

# ML
# torch+cu128 installed separately in install.bat
transformers==5.12.1
safetensors>=0.4.3
diffusers>=0.32.0
accelerate>=1.14.0

# Quantization (optional but recommended)
torchao>=0.4.0
gguf>=0.19.0

# PiD pixel diffusion decoder (optional high-VRAM upscale path)
git+https://github.com/nv-tlabs/PiD.git
hydra-core>=1.3.3
omegaconf>=2.3.0
attrs>=26.1.0
loguru>=0.7.3
fvcore>=0.1.5.post20221221
iopath>=0.1.10
imageio>=2.37.3
opencv-python-headless>=4.13.0.92
pandas>=3.0.3
sentencepiece>=0.2.1
wandb>=0.28.0
boto3>=1.43.38
botocore>=1.43.38

# Upscaling (OPTIONAL) — basicsr/realesrgan have no wheels for Python 3.13+
# and basicsr imports the removed torchvision.transforms.functional_tensor.
# The upscaler falls back to high-quality LANCZOS when these are absent, and
# the tiled-VAE / model-refine upscale paths don't need them at all. Install
# manually on Python <=3.12 for RealESRGAN:
#   pip install basicsr>=1.4.2 realesrgan>=0.3.0
